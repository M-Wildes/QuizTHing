{
	"name": "quiz-auth",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "vitest"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "^0.13.4",
		"vitest": "~3.2.0",
		"wrangler": "^4.77.0"
	},
	"dependencies": {
		"bcryptjs": "^3.0.3"
	}
}
